# Webscraper + RAG pipeline — runtime dependencies.
# Install with:  pip install -r requirements.txt
# For a CPU-only torch (much smaller image), see the Dockerfile.

# --- Web services ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9

# --- Frontend ---
streamlit>=1.38,<2.0
requests>=2.32,<3.0

# --- Config ---
python-dotenv>=1.0,<2.0

# --- Vector store ---
chromadb>=1.0,<2.0
numpy>=1.26

# --- Embeddings (local, CPU-friendly) ---
sentence-transformers>=3.0,<6.0
transformers>=4.44
torch>=2.2
einops>=0.8            # required by nomic-embed's remote code

# --- LLM (Groq, open-source models) ---
groq>=0.11,<1.0

# --- Scraping ---
crawl4ai>=0.7.4
playwright>=1.45
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
lxml-html-clean>=0.4
tldextract>=5.1
python-slugify>=8.0

# --- Chunking ---
tiktoken>=0.7

# --- Logging / CLI output ---
rich>=13.0
